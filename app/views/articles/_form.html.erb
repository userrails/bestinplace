<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :mydatetime %><br>
    <%= f.text_field :mydatetime, :class => "datepicker1" %>
  </div>

  <div class="field">
    <%= f.label :mydate %><br>
    <%= f.text_field :mydate, :class => "datepicker1" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script> 
$('.datepicker1').datepicker({
    format: 'yyyy-mm-dd'
});

$('.datepicker2').datepicker();
</script>


<script>
jQuery(function($){
  $.extend($.fn.datepicker.defaults, { format: 'yyyy-mm-dd' });
});
</script>